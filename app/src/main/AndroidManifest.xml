<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.arip.amalan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity"
            android:theme="@style/AppThemeFull"></activity>

        <activity android:name=".TasbihActivity"
            android:theme="@style/AppThemeTasbih"
            android:label="@string/secondary_nametasbih"
            />

        <activity android:name=".ZikirpagiActivity"
            android:label="Zikir Pagi"/>

        <activity android:name=".ZikirpetangActivity"
            android:label="Zikir Malam"/>


        <activity android:name=".ZikirsebelumtidurActivity"
            android:label="Bantuan"/>

        <activity android:name=".ayatkursipagiActivity"
            android:label="Zikir"/>
        <activity android:name=".alfalaqpagiActivity"
            android:label="Zikir"/>
        <activity android:name=".alikhlaspagiActivity"
            android:label="Zikir"/>
        <activity android:name=".annaaspagiActivity"
            android:label="Zikir"/>

        <activity
            android:name=".SplashActivity"
            android:theme="@style/AppThemeSplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SelesaiBacaActivity"
            android:theme="@style/AppThemeSplash"></activity>
    </application>

</manifest>